<div class="highlight high20">
  <div class="highlight-inner">
    <%= image_tag project.image.url(:thumb), class: 'project-thumbnail' %>
    <h5 class='text-center'><%= link_to project.title, project_path(project) %></h5>
    <p><%= project.blurb %></p>
  </div>
</div>